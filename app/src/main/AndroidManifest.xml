<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Lets us read which app/session is currently playing for the "attached to: X" status -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MotoEQ"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service that owns the live AudioEffect chain (Equalizer,
             BassBoost, LoudnessEnhancer) so it survives while you're riding and
             the screen/app is backgrounded. -->
        <service
            android:name=".effects.EffectForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- System-level broadcast: any app that supports external audio effects
             sends this when it opens/closes a playback session. This is the
             actual mechanism (no root needed) that YouTube Music, Spotify, etc.
             use to let EQ apps attach. Android explicitly exempts this action
             from the API 26+ implicit-broadcast restrictions, so a manifest
             receiver is allowed to catch it even while the app isn't running. -->
        <receiver
            android:name=".effects.AudioSessionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.action.OPEN_AUDIO_EFFECT_CONTROL_SESSION" />
                <action android:name="android.media.action.CLOSE_AUDIO_EFFECT_CONTROL_SESSION" />
            </intent-filter>
        </receiver>

        <!-- Restarts the effect service after a reboot if it was left enabled -->
        <receiver
            android:name=".effects.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
